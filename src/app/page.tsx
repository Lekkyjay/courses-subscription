export default function Home() {
  // const tasks = useQuery(api.tasks.getAllTasks);
  
  return (
    <div className="flex flex-col">
      {/* {tasks?.map(({ _id, text }) => <div key={_id}>{text}</div>)} */}
      <h1>Courses Subscriptions</h1>
    </div>
  );
}
